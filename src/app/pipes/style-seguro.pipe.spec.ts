import { StyleSeguroPipe } from './style-seguro.pipe';

describe('StyleSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
